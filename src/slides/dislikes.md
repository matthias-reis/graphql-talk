`(-)` query results are deeply nested objects

`(-)` asset size increases by 100k + x

`(-)` simultaneous requests are not managed

`(-)` POST requests

`(-)` slowest resolver determines the performance
